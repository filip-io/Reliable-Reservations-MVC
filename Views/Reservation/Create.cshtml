@model Reliable_Reservations_MVC.Models.Reservation.ReservationCreateViewModel
@{
    ViewData["Title"] = "Make a Reservation";
}

<h2>Make a Reservation</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger">
        <h4>Error</h4>
        <ul>
            @foreach (var modelState in ViewData.ModelState.Values)
            {
                foreach (var error in modelState.Errors)
                {
                    <li>@error.ErrorMessage</li>
                }
            }
        </ul>
    </div>
}

<form asp-action="Create" asp-controller="Reservation" method="post" id="reservationForm">
    <!-- Customer ID -->
    <div class="form-group mt-4">
        <label asp-for="CustomerId">Customer ID</label>
        <input asp-for="CustomerId" class="form-control" />
        <span asp-validation-for="CustomerId" class="text-danger"></span>
    </div>
    <!-- Number of Guests -->
    <div class="form-group mt-4">
        <label asp-for="NumberOfGuests">Number of Guests</label>
        <input asp-for="NumberOfGuests" type="number" id="numberOfGuests" class="form-control" min="1" max="8" />
        <span asp-validation-for="NumberOfGuests" class="text-danger"></span>
    </div>
    <!-- Select Date -->
    <div class="form-group mt-4">
        <label asp-for="ReservationDate">Select Date</label>
        <input type="date" id="reservationDate" class="form-control" min="@DateTime.Now.ToString("yyyy-MM-dd")" />
        <span asp-validation-for="ReservationDate" class="text-danger"></span>
    </div>
    <!-- Available Time Slots -->
    <div class="form-group mt-4">
        <label for="availableTimeSlots">Available Time Slots</label>
        <select id="availableTimeSlots" class="form-control" disabled>
            <option value="">Please select a date and number of guests before picking a time slot.</option>
        </select>
    </div>
    <!-- Available Tables -->
    <div class="form-group mt-4">
        <label for="availableTables">Available Tables</label>
        <div id="availableTables" class="form-control">
            <p>Please select a time slot before choosing tables.</p>
        </div>
    </div>
    <!-- Special Request -->
    <div class="form-group mt-4">
        <label asp-for="SpecialRequests">Special Request</label>
        <textarea asp-for="SpecialRequests" class="form-control" rows="3">None</textarea>
        <span asp-validation-for="SpecialRequests" class="text-danger"></span>
    </div>
    <!-- Hidden input for ReservationDate -->
    <input type="hidden" asp-for="ReservationDate" id="hiddenReservationDate" />
    <div class="form-group mt-4">
        <button type="submit" class="btn btn-primary" id="reserveButton" disabled>Make Reservation</button>
    </div>
</form>

@section Scripts {
    <script src="~/js/reservation.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
        // Pass required data to reservation.js and initialize the form
        document.addEventListener('DOMContentLoaded', function () {
            initializeReservationForm({
                allTables: @Html.Raw(ViewBag.TablesJson),
                openingHours: @Html.Raw(ViewBag.OpeningHoursJson),
                closedDays: @Html.Raw(ViewBag.ClosedDaysJson),
                getReservationsUrl: '@Url.Action("GetReservationsForDate", "Reservation")' // reservation.js need to handle this since it's dependent on the selected date
            });
        });
    </script>
}